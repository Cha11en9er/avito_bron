# Google Таблица и парсеры Avito

**Таблица:** `1vRF638f9LOKc8qkwTnjevUNYzT3VTKa4TY_kbULxjOg`  
**Секреты:** `.env` → `GOOGLE_CREDENTIALS_JSON`, `AVITO_GOOGLE_SHEET=1`

## Запуск

```bash
pip install -r requirements.txt
python -m parser                    # парсер Avito
python -m google_sheets seed-settings # заполнить лист «настройки»
python -m google_sheets seed-settings --force  # перезаписать настройки с нуля
python -m google_sheets seed-logs     # пересобрать «логи ежедневного парсинга» (заголовки итераций + URL)
```

## Почему лист «настройки» был пустой

Лист уже существовал в таблице с одной пустой строкой. Старый код при пустом `get_all_values()` падал с ошибкой до записи дефолтов. Сейчас:

- пустой лист **автоматически заполняется** при первом чтении;
- недостающие ключи **дописываются** без удаления ваших значений;
- можно принудительно: `python -m google_sheets seed-settings`.

Формат листа: **A = ключ**, **B = значение**, **C = описание**.

---

## Листы таблицы

| Лист | Назначение |
|------|------------|
| **ссылки** | Источник URL (столбец A) |
| **детальная информация** | Поля карточки + URL в A |
| **сдаваемость по дням** | `0` — свободно, `1` — занято; пусто — дата не на сайте. Пропуски между слотами до более поздних дат → `1` |
| **цены по дням** | Цены по датам |
| **настройки** | Режимы парсера |
| **логи ежедневного парсинга** | Время и статус после каждой ссылки |
| **детальная информация с кадастрами** | Кадастр (`kadastr/`) |

---

## Настройки (лист «настройки»)

**ID таблицы** задаётся только в файле `.env` (`AVITO_GOOGLE_SHEET_ID`), не на листе настроек.

Подробные пояснения в колонке **«описание»** на листе. Ниже — краткая шпаргалка. Пересоздать лист: `python -m google_sheets seed-settings --force`.

### Очередь и диапазон

| Ключ | Значение для обычной работы |
|------|-----------------------------|
| `sync_from_links_sheet` | **1** — список URL из листа «ссылки»; новые URL дописываются в конец рабочих листов |
| `detail_range_from` / `detail_range_to` | **0** и **0** — все ссылки; **0** и **10** — первые 10 |

### Что парсить

| Ключ | Значение |
|------|----------|
| `run_calendar` | **1** — бронь и цены (основное) |
| `run_detail` | **0** — детальная карточка не нужна каждый день |
| `detail_fill_mode` | **append** — догонять только пустые «название» (если включите `run_detail=1`) |
| `detail_only_empty_title` | **1** — вместе с append |
| `calendar_mode` | **daily** — не удалять старые строки; **primary** — один раз пересобрать листы брони/цен по «ссылки» (все старые данные стираются) |

### Итерации парсинга (3 слота в «логи ежедневного парсинга»)

Вместо привязки к календарным дням — **3 итерации** (три прогона). В строке 1 листа логов:

| Блок | Столбцы | Содержимое |
|------|---------|------------|
| 1 | C–F | ит.N, дата, время, статус (между блоками пустые B, G, L) |
| 2 | H–K | … |
| 3 | M–P | … |

| Ключ | Описание |
|------|----------|
| `parse_iteration` | Текущий номер итерации (можно менять вручную) |
| `iteration_progress` | Служебный счётчик (обновляется парсером). **Старт очереди** — по листу логов: первая ссылка без статуса в блоке текущей итерации; если логи стёрли — снова с начала диапазона |
| `iteration_slot_0/1/2` | Какой номер итерации отображается в каждом блоке логов |
| `iteration_status` | Служебно: `idle` — готов; `running` — идёт парсинг; `complete` — дошли до конца списка |

**Кольцо слотов:** итерация 4 → слоты `4, 2, 3`; итерация 5 → `4, 5, 3` (замена по формуле `(N-1) % 3`).

После **полного** прохода всех ссылок `parse_iteration` увеличивается на 1, `iteration_progress` сбрасывается в 0.

Пересборка листа (новая шапка + список URL по тем же правилам, что и очередь парсера): `python -m google_sheets seed-logs`. Старые значения в ячейках логов при этом **стираются**. После смены схемы столбцов пересоберите лист, иначе дата/статус сместятся.

### Диапазон ссылок (`detail_range_from` + `detail_range_to`)

Работает **всегда** (не нужен `detail_fill_mode=range`).

| from | to | Результат |
|------|-----|-----------|
| `0` | `0` | **Все** ссылки в выбранном источнике |
| `0` | `1` | Только **первая** ссылка (индекс 0) |
| `0` | `10` | Первые **10** ссылок (индексы 0…9) |
| `5` | `15` | Индексы 5…14 (10 штук) |

Интервал: **`[from, to)`** — правая граница не входит.

### Детальный парсер

| Ключ | По умолчанию | Описание |
|------|--------------|----------|
| `run_detail` | `0` | **1** — парсить «детальная информация» |
| `detail_fill_mode` | `append` | `primary` — пересоздать лист и все URL; `append` — только пустое «название»; `rebuild` — очистить и заново |
| `detail_only_empty_title` | `1` | При `append`: **1** — пропускать строки с заполненным «название»; **0** — парсить все в диапазоне |

### Календарь (сдаваемость + цены)

| Ключ | По умолчанию | Описание |
|------|--------------|----------|
| `run_calendar` | `1` | **1** — парсить сдаваемость и цены |
| `calendar_mode` | `daily` | `daily` — дописать даты ≥ сегодня; `primary` — пересоздать листы (нужен `sync_from_links_sheet=1`) |
| `calendar_start_date` | `03.05` | Старт столбцов при `primary` |
| `calendar_start_year` | `2026` | Год для заголовков `ДД.ММ` |
| `calendar_horizon_days` | `90` | Только для `calendar_mode=primary` (сетка дат при пересоздании) |
| `debug_dump_html` | `0` | `1` — HTML страницы после прокруток и брони в `debug_html_dir` |
| `debug_html_dir` | `debug_html` | Папка дампов от корня проекта |

**Сдаваемость / цены:** в ячейки пишутся дни от «сегодня» до последнего слота на сайте. Между свободными датами — `1`. Цены только для интервалов ровно в одну ночь (запись в день заезда).

Имена листов (`sheet_*`) — внизу листа «настройки», после пустых строк; менять только если переименовали вкладки в таблице.

---

## Изменили лист «ссылки»: удалили строки, добавили новые

Парсер **не переставляет** строки на листах «сдаваемость» / «цены» / «логи» в том же порядке, что в «ссылки», и **не удаляет** строки с URL, которых уже нет в «ссылки».

| Задача | Что сделать |
|--------|-------------|
| **Добавить** новые URL в конец рабочих листов | `sync_from_links_sheet=1`, `calendar_mode=daily`, запустить парсер — новые допишутся вниз |
| **Убрать** удалённые URL со всех листов разом | Один раз: `sync_from_links_sheet=1`, `calendar_mode=**primary**`, `run_calendar=1`, запустить парсер (листы «сдаваемость» и «цены» **пересоздаются** только по текущему «ссылки» — все 0/1 и цены нужно набрать заново). Затем вернуть `calendar_mode=daily` |
| **Логи** привести в порядок | `python -m google_sheets seed-logs` — шапка и список URL по «ссылки» (старые записи в логах стираются) |
| **Деталь** пересобрать по «ссылки» | `run_detail=1`, `detail_fill_mode=primary` (один раз), потом снова `run_detail=0` |

После правок в «ссылки» (удалили строку 34, сдвинулись номера): порядок строк на старых листах **не совпадёт** с номерами в «ссылки» — это нормально; важен столбец A (URL), не номер строки.

---

## Пример: тест сдаваемости и цен на 10 ссылок

На листе **«настройки»** выставьте:

| ключ | значение |
|------|----------|
| `run_detail` | `0` |
| `run_calendar` | `1` |
| `sync_from_links_sheet` | `0` |
| `calendar_mode` | `daily` |
| `detail_range_from` | `0` |
| `detail_range_to` | `10` |

Затем:

```bash
python -m google_sheets seed-settings   # если лист пустой
python -m parser
```

Поведение:

1. URL берутся из столбца A листа **«сдаваемость по дням»** (первые 10 строк с ссылками).
2. Лист **«ссылки»** не читается и на него ничего не копируется.
3. После каждой карточки обновляются **сдаваемость**, **цены** и **логи**.

Одна ссылка для проверки: `detail_range_from=0`, `detail_range_to=1`.

Полный прогон по всем URL на листе сдаваемости: `detail_range_from=0`, `detail_range_to=0`.

---

## Пример: только детальная информация (10 ссылок)

| ключ | значение |
|------|----------|
| `run_detail` | `1` |
| `run_calendar` | `0` |
| `sync_from_links_sheet` | `1` |
| `detail_fill_mode` | `append` |
| `detail_range_from` | `0` |
| `detail_range_to` | `10` |

---

## Семантика данных

### Сдаваемость

- `0` — ночь есть в карусели брони
- `1` — занято
- `нету на сайте` — объявление снято (все ячейки строки)

### Цены

- Строка с ценой за ночь; при снятом объявлении — `нету на сайте`

### Историчность

В ячейки с датой **≥ сегодня** (MSK) пишутся только новые значения; прошлые даты не перезаписываются (кроме случая «снято» — тогда вся строка).

---

## Структура кода

```
google_sheets/   # client, settings, links, detail, calendar, logs_daily, sync
parser/          # python -m parser
kadastr/         # отдельно
```

---

*Обновлено после рефакторинга: настройки, диапазоны, sync_from_links_sheet.*
